# Copy this file to .env and fill in your real credentials
# Place the .env file in the project root (same folder as index.html)

# Set to true to use SMTP via PHPMailer/Composer
USE_SMTP=true

# SMTP settings (only used when USE_SMTP=true)
SMTP_HOST=smtp.transip.email
SMTP_USERNAME=info@tkwebstudio.nl
SMTP_PASSWORD=Tahakaraman2006.
SMTP_PORT=465
SMTP_SECURE=ssl

# Mail addresses
FROM_EMAIL=info@tkwebstudio.nl
FROM_NAME="TK Webstudio"
TO_EMAIL=info@tkwebstudio.nl